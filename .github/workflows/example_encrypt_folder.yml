name: Generating, Zipping and Encrypting a folder/file

on: 
  workflow_dispatch:

jobs:
  build:
    name: Testing zipping and encryption
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout zip script
        uses: actions/checkout@v4
        with:
          path: .
          sparse-checkout: scripts/zip.sh
          sparse-checkout-cone-mode: false

      - name: Checkout encrypt script
        uses: actions/checkout@v4
        with:
          path: .
          sparse-checkout: scripts/encrypt.sh
          sparse-checkout-cone-mode: false
      
      - name: Create folder
        run: |
          mkdir test
          cd test
          touch testingfile
          ls
          cd ..
          ls

      - name: Zip folder
        run: ./scripts/zip.sh
        env:
          FOLDER: test

      - name: Encrypt build
        run: ./scripts/encrypt.sh
        env:
          PASSPHRASE: ${{ secrets.ARTIFACTS_PASSPHRASE }}
          FILE: ./test.zip

      - name: Overview
        run: |
          ls
          
      - name: Upload build results as artifact
        uses: actions/upload-artifact@v4
        with:
          name: testing_file
          path: test.zip.enc
